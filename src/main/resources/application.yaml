spring:
  application:
    name: ordish-ai
  datasource:
    url: jdbc:mysql://localhost:3306/ordishai?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root          # 改成你的数据库账号
    password: 123456          # 改成你的数据库密码
    driver-class-name: com.mysql.cj.jdbc.Driver
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      chat:
        options:
          model: deepseek-r1
      embedding:
        options:
          model: text-embedding-v3
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  data:
    redis:
      host: 192.168.170.132  # 例如 192.168.1.100 或公网IP
      port: 6379
      password: 123456 # 如果没设密码就留空或者删掉这一行
      database: 0
logging:
  level:
    org.springframework.ai.chat.client.advisor: debug
    com.ordish.ai: debug